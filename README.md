# 🗺️ 距离落差计算工具

基于 Leaflet 开发的地图测量工具，支持计算两点间的距离、海拔落差和坡度。

## ✨ 功能特性

- **🗺️ 多图层切换** - 普通地图 / 卫星地图 / 地形图（等高线）
- **📍 当前位置定位** - 获取并定位到用户当前位置
- **🔍 地点搜索** - 基于 OpenStreetMap 的地点搜索
- **📏 距离测量** - 点击两点计算直线距离和实际距离
- **📊 海拔落差** - 获取海拔高度并计算落差
- **📈 坡度计算** - 自动计算两点间坡度百分比
- **🎨 深色主题** - 精美的暗色系 UI 设计

## 🛠️ 技术栈

| 技术 | 说明 |
|------|------|
| UniApp + Vue 3 | 跨平台框架 |
| Leaflet | 地图引擎 |
| OpenStreetMap | 普通地图图源 |
| ArcGIS Online | 卫星地图图源 |
| OpenTopoMap | 地形图图源 |
| Nominatim | 地点搜索 API |
| Open-Meteo | 海拔高度 API |

## 🚀 快速开始

### 安装依赖

```bash
npm install --legacy-peer-deps
```

### 开发模式

```bash
npm run dev:h5
```

访问 http://localhost:5173

### 生产构建

```bash
npm run build:h5
```

构建产物在 `dist/build/h5` 目录。

## ☁️ Cloudflare Pages 部署

### 方式一：通过 Git 集成（推荐）

1. **推送代码到 GitHub/GitLab**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push origin main
   ```

2. **登录 Cloudflare Dashboard**
   - 访问 [Cloudflare Pages](https://dash.cloudflare.com/pages)
   - 点击 **Create a project**

3. **连接 Git 仓库**
   - 选择 **Connect to Git**
   - 授权并选择你的仓库

4. **配置构建设置**
   | 配置项 | 值 |
   |--------|-----|
   | Framework preset | None |
   | Build command | `npm run build:h5` |
   | Build output directory | `dist/build/h5` |
   | Root directory | `/` |

5. **环境变量（可选）**
   - `NODE_VERSION`: `18`

6. **点击 Save and Deploy**

### 方式二：通过 Wrangler CLI

1. **安装 Wrangler**
   ```bash
   npm install -g wrangler
   ```

2. **登录 Cloudflare**
   ```bash
   wrangler login
   ```

3. **构建项目**
   ```bash
   npm run build:h5
   ```

4. **部署到 Cloudflare Pages**
   ```bash
   wrangler pages deploy dist/build/h5 --project-name=distance-calculator
   ```

5. **首次部署会自动创建项目，后续部署会更新**

### 自定义域名

1. 在 Cloudflare Pages 项目设置中点击 **Custom domains**
2. 添加你的域名（需要先将域名添加到 Cloudflare）
3. Cloudflare 会自动配置 SSL 证书

## 📁 项目结构

```
55feishou-tools/
├── src/
│   ├── pages/
│   │   └── distance-calculator/
│   │       └── distance-calculator.vue  # 地图页面
│   ├── static/                          # 静态资源
│   ├── App.vue                          # 根组件
│   ├── main.js                          # 入口文件
│   ├── manifest.json                    # UniApp 配置
│   └── pages.json                       # 路由配置
├── package.json
├── vite.config.js
└── README.md
```

## 📖 使用说明

1. **切换地图类型** - 点击顶部的 `普通/卫星/地形` 按钮
2. **定位当前位置** - 点击左侧 `📍 定位` 按钮
3. **搜索地点** - 在搜索框输入地名并回车
4. **测量距离落差**：
   - 点击 `📏 测量` 按钮进入测量模式
   - 点击地图选择起点 (A)
   - 点击地图选择终点 (B)
   - 查看底部结果面板
5. **调整测量点** - 拖拽 A/B 标记可实时更新结果
6. **清除测量** - 点击 `🗑️ 清除` 按钮

## 📄 License

MIT
